<header class="navbar navbar-field-top navbar-inverse">
  <div class="container">
    <nav class="nav navbar-nav navbar-left">
      <span class="top">Bookers</span>
    </nav>
    <nav>
      <ul class="nav navbar-nav navbar-right">
        <li><%= link_to users_path do %><p class="glyphicon glyphicon-home home"> Home</p><% end %></li>
        <li><%= link_to "#" do %><p class="glyphicon glyphicon-link about"> About</p><%end%></li>
        <li><%= link_to new_user_registration_path do %><p class="glyphicon glyphicon-edit sign-up"> Sign up</p><%end%></li>
        <li><%= link_to new_user_session_path do %><p class="glyphicon glyphicon-log-in sign-in"> login</p><%end%></li>
      </ul>
    </nav>
  </div>
</header>

<h2>Forgot your password?</h2>

<%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
  <%= render "devise/shared/error_messages", resource: resource %>

  <div class="field">
    <%= f.label :email %><br />
    <%= f.email_field :email, autofocus: true, autocomplete: "email" %>
  </div>

  <div class="actions">
    <input type="submit" name="commit" value="Send me reset password instructions" class="btn btn-danger" data-disable-with="Send me reset password instructions">
  </div>
<% end %>

<%= render "devise/shared/links" %>

<footer>
<%= render 'shared/footer' %>
</footer>
